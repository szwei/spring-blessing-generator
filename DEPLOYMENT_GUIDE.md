# GitHub Pages 部署完整指南

## 🎯 重要说明

### 为什么会出现 404 错误？

1. **开发环境**：`/src/main.js` 是正确的路径，Vite 开发服务器会处理它
2. **生产环境**：需要先构建，Vite 会将所有文件打包到 `dist` 目录
3. **GitHub Pages**：需要部署 `dist` 目录中的文件，而不是 `src` 目录

## ✅ 正确的部署方式（推荐）

### 方法一：使用 GitHub Actions（最简单，自动构建）

这是推荐的方式，GitHub Actions 会自动构建并部署你的项目。

#### 步骤 1：确认文件存在

确保项目中有 `.github/workflows/deploy.yml` 文件。

#### 步骤 2：推送到 GitHub

```bash
cd E:\testAi\test1\spring-blessing-generator

# 运行构建和部署脚本
build-and-deploy.bat
```

或者手动执行：

```bash
# 添加所有文件
git add .

# 提交
git commit -m "部署：福满人间 - 春节祝福语生成器"

# 推送
git push
```

#### 步骤 3：在 GitHub 中配置

1. 访问你的 GitHub 仓库
2. 点击 `Settings` → `Pages`
3. 在 `Build and deployment` 部分：
   - **Source**: 选择 `GitHub Actions`
   - 会自动检测到 `.github/workflows/deploy.yml`
4. 保存

#### 步骤 4：等待部署

- GitHub Actions 会自动运行
- 需要 1-3 分钟完成构建和部署
- 可以在 `Actions` 标签查看进度

#### 步骤 5：访问网站

```
https://YOUR_USERNAME.github.io/spring-blessing-generator/
```

---

### 方法二：手动构建并使用 GitHub Pages（需要手动上传）

如果你不想使用 GitHub Actions，可以手动构建并上传。

#### 步骤 1：本地构建

```bash
cd E:\testAi\test1\spring-blessing-generator

# 清理旧文件
rmdir /s /q dist

# 构建
npm run build
```

#### 步骤 2：检查构建结果

```bash
# 查看构建结果
dir dist
```

应该看到：
```
dist/
  ├── index.html
  └── assets/
      ├── index-xxxxx.js
      └── index-xxxxx.css
```

#### 步骤 3：将构建结果推送到 gh-pages 分支

```bash
# 切换到新分支
git checkout -b gh-pages

# 复制 dist 文件到根目录
xcopy dist\*.* . /E /I /Y

# 删除不需要的文件
rmdir /s /q src
rmdir /s /q node_modules
del package.json
del vite.config.js
del tailwind.config.js
del postcss.config.js

# 提交
git add .
git commit -m "部署到 gh-pages"

# 推送
git push origin gh-pages
```

#### 步骤 4：在 GitHub 中配置

1. 访问你的 GitHub 仓库
2. 点击 `Settings` → `Pages`
3. 在 `Build and deployment` 部分：
   - **Source**: 选择 `Deploy from a branch`
   - **Branch**: 选择 `gh-pages`
   - **Folder**: 选择 `/ (root)`
4. 保存

#### 步骤 5：访问网站

```
https://YOUR_USERNAME.github.io/spring-blessing-generator/
```

---

## 🔧 验证部署

### 检查 1：打开浏览器开发者工具

1. 访问你的网站
2. 按 `F12` 打开开发者工具
3. 查看 `Console` 标签
4. 如果没有 404 错误，说明部署成功

### 检查 2：查看 Network 标签

1. 在开发者工具中切换到 `Network` 标签
2. 刷新页面
3. 查看加载的资源
4. 确认没有红色的 404 错误

### 检查 3：查看 GitHub Actions 日志（如果使用方法一）

1. 访问 GitHub 仓库
2. 点击 `Actions` 标签
3. 查看最近的 workflow 运行记录
4. 点击查看详细日志

---

## ❌ 错误的原因和解决方案

### 错误 1：看到 `/src/main.js` 404

**原因**：
- 服务器直接提供源代码文件，而不是构建后的文件
- GitHub Pages 没有正确配置

**解决方案**：
- 使用 GitHub Actions（方法一）自动构建
- 或手动构建并上传到 gh-pages 分支（方法二）

### 错误 2：dist 目录中的文件不正确

**原因**：
- 构建失败
- `vite.config.js` 配置错误

**解决方案**：
```bash
# 检查构建
npm run build

# 检查输出
dir dist

# 应该看到：
# dist/index.html
# dist/assets/index-xxxxx.js
# dist/assets/index-xxxxx.css
```

### 错误 3：构建成功但部署后还是 404

**原因**：
- GitHub Pages 配置错误
- 选择了错误的分支或目录

**解决方案**：
```
Settings → Pages → Build and deployment:

如果使用 GitHub Actions:
  Source: GitHub Actions

如果使用手动部署:
  Source: Deploy from a branch
  Branch: gh-pages
  Folder: / (root)
```

---

## 🚀 快速开始（推荐）

使用我们提供的一键部署脚本：

```bash
cd E:\testAi\test1\spring-blessing-generator

# Windows 用户
build-and-deploy.bat

# 按提示操作即可
```

脚本会自动：
1. 清理旧文件
2. 安装依赖
3. 构建项目
4. 提交到 Git
5. 推送到 GitHub
6. 提示配置 GitHub Pages

---

## 📋 部署检查清单

在部署前，请确认：

- [ ] 已在 GitHub 创建公开仓库
- [ ] 仓库名是 `spring-blessing-generator`
- [ ] 已将代码推送到 GitHub
- [ ] `.github/workflows/deploy.yml` 文件存在
- [ ] `vite.config.js` 配置正确
- [ ] 本地构建成功（`npm run build`）
- [ ] `dist` 目录包含 `index.html` 和 `assets` 文件夹

部署后，请确认：

- [ ] GitHub Actions 运行成功（如果使用方法一）
- [ ] GitHub Pages 配置正确
- [ ] 网站可以访问
- [ ] 没有 404 错误
- [ ] 功能正常

---

## 🆘 需要帮助？

如果问题仍未解决：

1. 检查 `fix-404.md` 获取更多解决方案
2. 查看 GitHub Actions 日志
3. 检查浏览器控制台的具体错误信息
4. 确认 `vite.config.js` 中的 `base` 配置

---

## 🎉 部署成功标志

当以下条件都满足时，说明部署成功：

1. ✅ 网站可以正常访问
2. ✅ 没有 404 错误
3. ✅ 祝福语生成功能正常
4. ✅ 复制、分享、保存功能正常
5. ✅ 页面样式正常显示

恭喜！你的春节祝福语生成器已经成功部署！🧧✨